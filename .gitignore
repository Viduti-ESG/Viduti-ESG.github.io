# Build output (generated by build_assets.py — deploy artifact, not source)
build/

# Local env / runtime
.env
*.log
greencurve.db
greencurve.db-wal
greencurve.db-shm
__pycache__/
*.pyc

# Reconciliation 2026-06-25: exclude on-server backups / venv / db dumps
_dq_backups/
venv/
*.bak
*.bak-*
*.bak.*
*-orig.js
.nav-backup-*/
uploads/
*.bak_*

# Local pipeline intermediates — regenerated by tools/ scripts on each run.
# NOTE: the *_deploy.json variants and tools/rescored.json are deploy
# transports (read by apply_* scripts ON THE SERVER) and must stay tracked.
tools/bottlenecks_extracted.json
tools/emissions_extracted.json
tools/sectors_extracted.json
tools/raw_features.json
posts_extracted.json
blog/
.climate_agent/
